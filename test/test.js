'use strict';

var expect = require('chai').expect;
var serialize = require('../index');

describe('#serialize', function() {
  it('should mask object with attributes only');
  it('should call object.attr() when exist');
  it('should pass options to object.attr()')
});